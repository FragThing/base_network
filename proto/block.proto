syntax = "proto3";

// The BasicBlock message represents the basic structure of a block.
// It includes the protocol, hash of the previous block, and data.
message BasicBlock {
  string previous_block_hash = 1; // The hash of the previous block
  string protocol = 2;            // The protocol used
  bytes data = 3;                 // The data contained in the block
}
